using System;
using StackErp.ViewModel.ViewContext;

namespace StackErp.ViewModel.Services
{
    public class Evaluator
    {
        public bool EvaluateCondition(FormContext formContext, object filters) //configured filter json
        {
            return true;
        }
    }
}
