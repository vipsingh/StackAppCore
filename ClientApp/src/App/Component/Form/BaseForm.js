import React from "react";
import PropTypes from "prop-types";
import _ from "lodash";
import PageView from "../PageView";

export default class BaseForm extends PageView {

    constructor(props) {
        super(props);
    }    
}